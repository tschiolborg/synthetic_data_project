train:
  epochs: 10
  lr: 0.001
  dataset:
    img_dir: ${hydra:runtime.cwd}/data/images/train
    anno_dir: ${hydra:runtime.cwd}/data/annotations
    classes:
    - _
    - regulatory--stop--g1
    use_single_annotation_file: false
    label_map:
      _: 0
      regulatory--stop--g1: 1
  dataloader:
    shuffle: true
    batch_size: 2
    num_workers: 4
  dataset_val:
    img_dir: ${hydra:runtime.cwd}/data/images/val
    anno_dir: ${hydra:runtime.cwd}/data/annotations
    classes:
    - _
    - regulatory--stop--g1
    use_single_annotation_file: false
    label_map:
      _: 0
      regulatory--stop--g1: 1
  dataloader_val:
    shuffle: false
    batch_size: 1
    num_workers: 4
dev:
  dataset:
    img_dir: ${hydra:runtime.cwd}/data/images/train
    anno_dir: ${hydra:runtime.cwd}/data/annotations
    classes:
    - _
    - regulatory--stop--g1
    use_single_annotation_file: false
    label_map:
      _: 0
      regulatory--stop--g1: 1
  dataloader:
    shuffle: true
    batch_size: 2
    num_workers: 4
model:
  name: faster-rcnn
  num_classes: 2
  weights: ???
