datamodule:
  train:
    dir: data/MTSD/images/train
    batch_size: 1
    num_workers: 0
    pin_memory:
    transforms:
  val:
    dir: data/MTSD/images/val
    batch_size: 1
    num_workers: 0
    pin_memory:
    transforms:
  test:
  anno_dir: data/MTSD/annotations
  classes_path: data/classes/single.json
  num_classes: 2
  include_negative_examples: false

training:
  debug: false

train:
  lr: 0.001
