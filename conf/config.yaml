datamodule:
  train:
    dir: data/images/train
    batch_size: 4
    num_workers: 0
    pin_memory:
    transforms:
  val:
    dir: data/images/val
    batch_size: 1
    num_workers: 0
    pin_memory:
    transforms:
  test:
  anno_dir: data/annotations
  classes_path: data/classes/classes.json
  include_negative_examples: true

training:
  debug: false
